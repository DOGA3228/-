# 🏭 Система управления мебельным производством

[![NestJS](https://img.shields.io/badge/Backend-NestJS-red?logo=nestjs)](https://nestjs.com)
[![React](https://img.shields.io/badge/Frontend-React-blue?logo=react)](https://react.dev)
[![PostgreSQL](https://img.shields.io/badge/Database-PostgreSQL-336791?logo=postgresql)](https://postgresql.org)

Полнофункциональная full-stack система для управления производством мебели. Включает REST API на NestJS, React интерфейс и PostgreSQL базу данных.

## 📋 Содержание

- [🎯 Описание](#описание)
- [✨ Функционал](#функционал)
- [🛠️ Требования](#требования)
- [🚀 Установка](#установка)
- [✅ Проверка](#проверка)
- [📁 Структура](#структура)
- [🔌 API](#api)
- [🗄️ База данных](#база-данных)
- [🐛 Решение проблем](#решение-проблем)

---

## 🎯 Описание

Учебный проект для демонстрации full-stack разработки. Система позволяет:
- Управлять каталогом мебели (добавление, редактирование, удаление)
- Просматривать информацию о цехах и материалах
- Рассчитывать необходимое сырье с учетом потерь материалов
- Получать данные через REST API с автоматической документацией

Все компоненты (БД, Backend, Frontend) полностью интегрированы и взаимодействуют через реальные HTTP запросы.

---

## ✨ Функционал

### 📦 Управление продуктами
- Просмотр всех товаров (диван, стол, стул)
- Добавление новых продуктов
- Редактирование характеристик
- Удаление из каталога
- Поиск и фильтрация

### 🏢 Справочники
- **Цеха** — список производственных цехов с информацией
- **Материалы** — справочник материалов с коэффициентами потерь
- **Типы продукции** — классификация товаров

### 🧮 Калькулятор
- Расчет необходимого сырья для заказа
- Учет коэффициентов потерь при обработке материалов
- Пошаговые расчеты с итоговым отчетом

### 📊 Интеграция
- Все данные из PostgreSQL
- REST API с автоматической Swagger документацией
- Адаптивный интерфейс (Desktop и Mobile)
- Обработка ошибок и загрузки

---

## 🛠️ Требования

| Компонент | Версия | Как установить |
|-----------|--------|-----------------|
| Node.js | 18+ LTS | [nodejs.org](https://nodejs.org) |
| PostgreSQL | 14+ | [postgresql.org](https://postgresql.org) |
| Git | 2.30+ | [git-scm.com](https://git-scm.com) |

**Проверить установку:**
```bash
node --version      # v18+
npm --version       # 9+
psql --version      # 14+
git --version       # 2.30+
```

---

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/DOGA3228/-.git
cd -
```

### 2. Создание базы данных
```bash
# Подключиться к PostgreSQL
psql -U postgres

# Создать базу
CREATE DATABASE furniture_management;
\q

# Если есть SQL файлы с данными:
psql -U postgres -d furniture_management < backend/database/schema.sql
```

### 3. Запуск Backend

**Терминал 1:**
```bash
cd backend

# Установка зависимостей
npm install

# Создание .env файла
cp .env.example .env

# Отредактируйте .env файл со своими параметрами:
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASS=ваш_пароль_postgresql
# DB_NAME=furniture_management
# PORT=3000

# Синхронизация БД
npm run typeorm:sync

# Запуск сервера
npm run start:dev
```

✅ Backend работает на `http://localhost:3000`

**Проверить:**
- Swagger документация: `http://localhost:3000/api`
- API ответ: `http://localhost:3000/api/products`

### 4. Запуск Frontend

**Терминал 2 (оставить backend запущенным!):**
```bash
cd frontend

# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

✅ Frontend работает на `http://localhost:5173`

---

## ✅ Проверка

Откройте браузер и перейдите на `http://localhost:5173`

**Проверьте следующее:**

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Загрузится главная страница | Статистика и основная информация |
| 2 | Клик на "Продукты" | Список товаров из БД |
| 3 | Нажать кнопку "Добавить" | Откроется форма для нового продукта |
| 4 | Добавить новый товар | Товар появляется в списке |
| 5 | Нажать "Цеха" | Карточки всех цехов производства |
| 6 | Нажать "Материалы" | Справочник материалов с потерями |
| 7 | Нажать "Калькулятор" | Можно рассчитать сырье |
| 8 | Открыть DevTools (F12) | Нет ошибок в консоли |

**В DevTools → Network:**
- GET запросы к `localhost:3000/api/products`
- GET запросы к `localhost:3000/api/workshops`
- GET запросы к `localhost:3000/api/materials`

---

## 📁 Структура

```
furniture-management/
│
├── backend/                      # NestJS REST API
│   ├── src/
│   │   ├── controllers/          # REST эндпоинты
│   │   │   ├── products.controller.ts
│   │   │   ├── workshops.controller.ts
│   │   │   └── materials.controller.ts
│   │   ├── entities/             # Модели БД (TypeORM)
│   │   │   ├── product.entity.ts
│   │   │   ├── workshop.entity.ts
│   │   │   └── material.entity.ts
│   │   ├── services/             # Бизнес-логика
│   │   │   ├── products.service.ts
│   │   │   └── calculator.service.ts
│   │   ├── app.module.ts
│   │   └── main.ts               # Точка входа (CORS)
│   ├── database/
│   │   ├── schema.sql            # Схема БД
│   │   └── data.sql              # Тестовые данные
│   ├── package.json
│   ├── .env.example
│   └── tsconfig.json
│
├── frontend/                     # React + Vite
│   ├── src/
│   │   ├── components/
│   │   │   ├── Header.jsx        # Навигация
│   │   │   ├── Sidebar.jsx       # Меню
│   │   │   ├── HomePage.jsx      # Главная
│   │   │   ├── ProductsPage.jsx  # Управление товарами
│   │   │   ├── WorkshopsPage.jsx # Цеха
│   │   │   ├── MaterialsPage.jsx # Материалы
│   │   │   └── CalculatorPage.jsx# Калькулятор
│   │   ├── App.jsx               # Главный компонент
│   │   ├── App.css               # Стили
│   │   ├── api.js                # HTTP клиент
│   │   ├── main.jsx
│   │   └── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── .env.example
│
├── отчеты/                       # Учебная документация
│   ├── Report-Task-1.md          # СУБД и проектирование
│   ├── Report-Task-2.md          # Backend разработка
│   ├── Report-Task-3.md          # Frontend разработка
│   └── Report-Task-4.md          # Интеграция и тесты
│
└── README.md                     # Этот файл
```

---

## 🔌 API

### Документация (Swagger)
```
http://localhost:3000/api
```

Все эндпоинты с описанием, примерами и параметрами.

### Основные эндпоинты

**Продукты:**
```bash
GET    /api/products              # Список всех продуктов
GET    /api/products/:id          # Один продукт
POST   /api/products              # Создать продукт
PUT    /api/products/:id          # Обновить продукт
DELETE /api/products/:id          # Удалить продукт
```

**Справочники:**
```bash
GET    /api/workshops             # Все цеха
GET    /api/materials             # Все материалы
GET    /api/product-types         # Типы продукции
```

**Примеры запросов:**

```bash
# Получить все продукты
curl http://localhost:3000/api/products

# Создать новый продукт
curl -X POST http://localhost:3000/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Диван угловой",
    "type_id": 1,
    "material_id": 2,
    "workshop_id": 1,
    "price": 25000
  }'

# Обновить продукт
curl -X PUT http://localhost:3000/api/products/1 \
  -H "Content-Type: application/json" \
  -d '{"name": "Диван угловой (обновлено)", "price": 28000}'

# Удалить продукт
curl -X DELETE http://localhost:3000/api/products/1
```

---

## 🗄️ База данных

### Таблицы

**products** — основной каталог товаров
```sql
id, name, type_id, material_id, workshop_id, price, created_at
```

**product_types** — типы товаров
```sql
id, name ("диван", "стол", "стул")
```

**materials** — материалы с коэффициентами потерь
```sql
id, name, loss_percent, unit
```

**workshops** — цеха производства
```sql
id, name, capacity_hours, equipment_type
```

### Связи

```
products → product_types (FK type_id)
products → materials (FK material_id)
products → workshops (FK workshop_id)
```

### Нормализация

✅ **3 нормальная форма (3NF)**
✅ **Внешние ключи** для ссылочной целостности
✅ **Индексы** на часто запрашиваемые поля
✅ **Constraints** для валидации данных

---

## 🐛 Решение проблем

### ❌ CORS ошибка

```
Access to XMLHttpRequest at 'http://localhost:3000/api/products' 
from origin 'http://localhost:5173' has been blocked
```

**Решение:**
1. Убедитесь, что backend запущен первым
2. Проверьте в `backend/src/main.ts`:
```typescript
app.enableCors({
  origin: 'http://localhost:5173',
  credentials: true,
});
```
3. Перезагрузите браузер (Ctrl+Shift+Delete или Cmd+Shift+Delete)

---

### ❌ Port 3000 или 5173 уже занят

```
Error: listen EADDRINUSE :::3000
```

**Решение:**

**Mac/Linux:**
```bash
lsof -i :3000
kill -9 <PID>
```

**Windows:**
```bash
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

Или измените PORT в `.env`:
```
PORT=3001
```

---

### ❌ Ошибка подключения к БД

```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Решение:**

1. Проверьте, запущен ли PostgreSQL:
```bash
# Mac
brew services start postgresql

# Windows (из Services или):
pg_ctl -D "C:\Program Files\PostgreSQL\15\data" start
```

2. Проверьте параметры в `.env`:
```
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=ваш_пароль
DB_NAME=furniture_management
```

3. Проверьте подключение:
```bash
psql -U postgres -d furniture_management -c "SELECT 1"
```

---

### ❌ npm install ошибки

```
npm ERR! code ERESOLVE
npm ERR! ERESOLVE unable to resolve dependency tree
```

**Решение:**
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install

# Или с флагом
npm install --legacy-peer-deps
```

---

### ❌ TypeORM синхронизация не работает

```
Error: Cannot find entity
```

**Решение:**
```bash
cd backend

# Удалить и пересоздать БД
psql -U postgres -c "DROP DATABASE furniture_management;"
psql -U postgres -c "CREATE DATABASE furniture_management;"

# Синхронизировать заново
npm run typeorm:sync
```

---

### ❌ Frontend не видит Backend

Frontend показывает ошибку при загрузке данных.

**Решение:**
1. Проверьте, запущен ли backend:
```bash
curl http://localhost:3000/api
```

Должна вернуться Swagger страница.

2. Проверьте URL в `frontend/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:3000/api';
```

3. Откройте DevTools (F12) → Console → посмотрите ошибки

---

## 📚 Документация

В папке `отчеты/`:

- **Report-Task-1.md** — база данных и проектирование (СУБД, 3NF, ER-диаграмма)
- **Report-Task-2.md** — backend разработка (NestJS, REST API, интеграция с БД)
- **Report-Task-3.md** — frontend разработка (React, компоненты, маршруты)
- **Report-Task-4.md** — интеграция и тестирование (CRUD цикл, примеры)

---

## 📊 Технологический стек

**Backend:**
- NestJS 10 — фреймворк для Node.js
- TypeORM — ORM для работы с БД
- PostgreSQL 15 — реляционная БД
- Swagger — документация API

**Frontend:**
- React 18 — библиотека интерфейсов
- Vite 5 — сборщик с горячей перезагрузкой
- React Router — маршрутизация страниц
- TypeScript — типизированный JavaScript

---

## 🎓 Для преподавателя

**Демонстрация (5 минут):**

1. Запустить backend и frontend (см. выше)
2. Открыть `localhost:5173` в браузере
3. Показать все 5 страниц приложения
4. Добавить/отредактировать/удалить продукт (CRUD)
5. Открыть Swagger: `localhost:3000/api`
6. Проверить БД:
```bash
psql -U postgres -d furniture_management
SELECT * FROM products;
\dt                 # список всех таблиц
```

**Итоговое описание:**
> Это система управления производством мебели. Данные хранятся в PostgreSQL, backend на NestJS предоставляет REST API с документацией, frontend на React отображает продукты, цеха и материалы, а также рассчитывает необходимое сырье с учетом потерь. Все операции (добавление, редактирование, удаление) выполняются через реальные HTTP запросы.

---

**📅 Дата создания:** Декабрь 2025  
**👤 Автор:** DOGA3228  
**🔗 Репозиторий:** [github.com/DOGA3228/-](https://github.com/DOGA3228/-)

✨ *Система полностью функциональна и готова к демонстрации.*
